export class State128D { constructor(values){this.values=Float64Array.from(values??Array(128).fill(0));if(this.values.length!==128)throw new RangeError('128D state requires 128 coordinates')} add(other){return new State128D(this.values.map((v,i)=>v+other.values[i]))} scale(k){return new State128D(this.values.map(v=>v*k))} norm(){return Math.sqrt(this.values.reduce((s,v)=>s+v*v,0))} snapshot(){return Array.from(this.values)} }
export class BrainNetwork { constructor(){this.nodes=new Map();this.edges=new Map();this.time=0} addNode(id,state=new State128D()){this.nodes.set(id,state);if(!this.edges.has(id))this.edges.set(id,[])} connect(a,b,weight=1){if(!this.edges.has(a))this.edges.set(a,[]);this.edges.get(a).push({to:b,weight})} step(){const next=new Map([...this.nodes].map(([id,s])=>[id,new State128D(s.values)]));for(const [id,edges] of this.edges){let acc=new State128D();for(const e of edges){const target=this.nodes.get(e.to);if(target)acc=acc.add(target.scale(e.weight))}next.set(id,next.get(id).add(acc.scale(0.01)))}this.nodes=next;this.time++;return this.trace()} trace(){return{time:this.time,nodes:[...this.nodes].map(([id,s])=>({id,norm:s.norm()}))}} }
export class DeterministicTrace { constructor(){this.events=[]} record(type,payload){this.events.push({seq:this.events.length,type,payload})} export(){return JSON.stringify(this.events)} }
