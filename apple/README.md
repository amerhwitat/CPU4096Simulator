# Apple application

Native SwiftUI/Xcode shell for CPU4096Simulator. Simulator/ISA/network/128D model logic remains separated from the Apple UI layer.

Targets: iOS/iPadOS (`iosArm64`, `iosSimulatorArm64`) and macOS. IPA generation requires macOS/Xcode; existing Node.js/JavaScript runtime remains intact.
