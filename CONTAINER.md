# Containerized build environment

```bash
docker compose build
docker compose up
```

The container provides a reproducible C/C++/CMake/Python environment. Native simulator/GUI execution remains available outside Docker.
