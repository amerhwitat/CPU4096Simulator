# Cross-language simulator workloads

The simulator is the Node.js/web execution track and exchanges deterministic JSON/JSONL vectors with Python, Java and C++. Traces record ISA width, vector shape, seed, dataset hash and model version. This enables reproducible comparison across host implementations.