# Web Implementations

The simulator web layer provides a browser step-loop and PHP metadata boundary. Future WebAssembly builds should share instruction fixtures with the native simulator and provide scalar fallbacks where SIMD features are unavailable.