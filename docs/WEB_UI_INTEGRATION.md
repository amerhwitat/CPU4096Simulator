# Chimera II Web UI Integration

The Web UI may visualize the CPU4096 simulator through a read-only adapter: registers, memory, ISA execution state, traces and performance telemetry. Browser code must not gain arbitrary host process, filesystem, credential or network privileges.

Canonical OS/UI integration: `amerhwitat/ChimeraIIOS`; Web integration/conformance: `amerhwitat/test`.
