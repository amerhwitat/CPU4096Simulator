# Web UI Simulator Integration

Expose CPU4096 simulation state to `amerhwitat/test` through a versioned, read-only visualization contract. The Aurora Three.js desktop may render processor state, ISA operations, memory, and execution traces; browser code must not execute arbitrary native instructions or access the host outside an authorized backend.
