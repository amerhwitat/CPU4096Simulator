# CPU4096Simulator Mobile Communications

Kotlin Android exposes the simulator's synchronized conversation and device media boundary. Voice/video transport is designed for WebRTC; text state uses monotonic sequence and SHA-256 integrity checks. The mobile layer remains separate from CPU emulation semantics.
